services:
  - type: web
    name: logistic-web
    runtime: node
    buildCommand: cd logistics && npm install
    startCommand: cd logistics && npm start
    envVars:
      - key: NODE_ENV
        value: production